name: book_swap
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^3.1.1
  firebase_auth: ^5.1.1
  cloud_firestore: ^5.0.2
  # firebase_storage: ^12.1.0  <-- This is now removed

  # State Management
  flutter_riverpod: ^2.5.1

  # Utilities
  image_picker: ^1.1.2

  # Cloudinary
  cloudinary_public: ^0.23.1 # <-- This is added

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true